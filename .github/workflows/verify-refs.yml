name: verify-refs

on:
  pull_request:
    paths:
      - 'agents/**/*.toml'
      - 'dreamers/**/*.md'
      - 'skills/**/*.md'
      - 'scripts/sync-refs.ps1'
      - 'scripts/sync-refs.sh'
      - 'scripts/Test-DreamersCodex.ps1'
      - 'scripts/Test-DreamersCodex.sh'
      - '.github/workflows/verify-refs.yml'
  push:
    branches: [main]
    paths:
      - 'agents/**/*.toml'
      - 'dreamers/**/*.md'
      - 'skills/**/*.md'
      - 'scripts/sync-refs.ps1'
      - 'scripts/sync-refs.sh'
      - 'scripts/Test-DreamersCodex.ps1'
      - 'scripts/Test-DreamersCodex.sh'
      - '.github/workflows/verify-refs.yml'
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - name: Verify inlined refs match source
        shell: bash
        run: |
          scripts/sync-refs.sh -Verify
          scripts/Test-DreamersCodex.sh
